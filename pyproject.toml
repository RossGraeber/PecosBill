[project]
name = "pecosbill"
version = "0.1.0"
description = "Parametric cyclonic dust separator generator for a CNC router vacuum line"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "build123d>=0.11.1",
]
